{
  "name": "@specforge/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'api app scaffold is not initialized yet'",
    "build": "echo 'api build is not initialized yet'",
    "lint": "echo 'api lint is not initialized yet'",
    "test": "echo 'api test is not initialized yet'",
    "format": "echo 'api format is not initialized yet'"
  }
}
