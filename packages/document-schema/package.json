{
  "name": "@specforge/document-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo 'document-schema lint is not initialized yet'",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "echo 'document-schema format is not initialized yet'"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vitest": "^2.1.0"
  }
}
